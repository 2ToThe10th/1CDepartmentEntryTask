dist: bionic
sudo: required

language: cpp

os:
  - linux

compiler:
  - gcc

script:
  - mkdir cmake-build
  - cd cmake-build
  - cmake ../
  - make

notifications:
  email:
    recipients:
      - grib.a@phystech.edu
    on_success: always
    on_failure: always